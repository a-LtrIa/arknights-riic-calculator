#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
修复第81-100个干员类的问题
"""

import re

# 读取文件
with open('/Users/altria/Documents/code/arkproject/building-calculator/core/operator_classes.py', 'r', encoding='utf-8') as f:
    content = f.read()

# 1. 修复杏仁 - E2黑钢国际联动
old_杏仁 = '''class 杏仁(干员基类):
    """
    明日方舟 杏仁 基建技能类
        精英0: 小奇思 - 进驻制造站时，贵金属类配方的生产力+25%，心情每小时消耗+0.25...
    精英2: 挑大梁 - 进驻制造站时，基建内（不包含副手及活动室使用者）每有1名黑钢国际术语:黑钢国际包含以下干员雷蛇、芙兰...
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器):
        super().__init__(精英等级, 特殊变量)
        self.特殊变量列表 = []
        if 精英等级 >= 2:
            self.基本属性 = {"生产力": 2}
        else:
            self.基本属性 = {}

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        return self.基本属性.get("生产力", 0)'''

new_杏仁 = '''class 杏仁(干员基类):
    """
    明日方舟 杏仁 基建技能类
        精英0: 小奇思 - 进驻制造站时，贵金属类配方的生产力+25%，心情每小时消耗+0.25
    精英2: 挑大梁 - 进驻制造站时，基建内每有1名黑钢国际干员+2%生产力
            黑钢国际: 雷蛇、芙兰卡、杰西卡、香草、杏仁、寻澜
    """
    黑钢国际成员 = ["雷蛇", "芙兰卡", "杰西卡", "香草", "杏仁", "寻澜"]

    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器, 配置数据: dict = None):
        super().__init__(精英等级, 特殊变量, 配置数据)
        self.特殊变量列表 = []
        # E0: 贵金属+25%，心情消耗+0.25
        self.基本属性 = {"贵金属生产力": 25, "心情消耗": 0.25}

    def 计算黑钢国际人数(self) -> int:
        """计算基建内黑钢国际成员数量（不含副手及活动室）"""
        if not self.配置数据:
            return 0
        count = 0
        排除设施 = ["控制中枢", "活动室"]
        for 设施名, 设施数据 in self.配置数据.items():
            # 排除控制中枢和活动室
            if any(排除 in 设施名 for 排除 in 排除设施):
                continue
            if isinstance(设施数据, dict):
                进驻干员列表 = 设施数据.get("进驻干员", [])
                for op in 进驻干员列表:
                    if op.get("名称") in self.黑钢国际成员:
                        count += 1
        return count

    def 计算特殊技能(self):
        pass

    def 计算效率(self, 配方类型: str = "") -> float:
        """
        E0: 贵金属配方+25%
        E2: 每有1名黑钢国际干员+2%
        """
        基础效率 = 0.0
        if 配方类型 == "贵金属":
            基础效率 = 25.0

        if self.精英等级 >= 2:
            黑钢人数 = self.计算黑钢国际人数()
            return 基础效率 + 黑钢人数 * 2.0
        return 基础效率'''

content = content.replace(old_杏仁, new_杏仁)

# 2. 修复水月 - E0标准化类技能联动
old_水月 = '''class 水月(干员基类):
    """
    明日方舟 水月 基建技能类
        精英0: 意识协议 - 进驻制造站时，当前制造站内每个标准化类技能术语:标准化类技能包含以下技能标准化·α、标准化·β为自身...
    精英2: 标准化·β - 进驻制造站时，生产力+25%...
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器):
        super().__init__(精英等级, 特殊变量)
        self.特殊变量列表 = []
        if 精英等级 >= 2:
            self.基本属性 = {"生产力": 25}
        else:
            self.基本属性 = {}

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        return self.基本属性.get("生产力", 0)'''

new_水月 = '''class 水月(干员基类):
    """
    明日方舟 水月 基建技能类
        精英0: 意识协议 - 进驻制造站时，当前制造站内每个标准化类技能为自身+5%生产力
            标准化类技能: 标准化·α、标准化·β
            拥有者: 香草/杰西卡(E0)、芬/翎羽(E0)、泡普卡(E0)、清流(E1)、星源(E0/E2)、水月(E2)等
    精英2: 标准化·β - 进驻制造站时，生产力+25%
    """
    # 标准化类技能及其拥有者（部分常见干员）
    标准化技能拥有者 = {
        "香草": (0, "标准化·α"),
        "杰西卡": (0, "标准化·β"),
        "芬": (0, "标准化·α"),
        "翎羽": (0, "标准化·α"),
        "泡普卡": (0, "标准化·α"),
        "清流": (1, "标准化·β"),
        "星源": (0, "标准化·α"),  # E0:α, E2:β
        "水月": (2, "标准化·β"),
    }

    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器, 配置数据: dict = None, 当前设施名: str = None):
        super().__init__(精英等级, 特殊变量, 配置数据, 当前设施名)
        self.特殊变量列表 = []
        self.基本属性 = {}

    def 计算标准化数量(self) -> int:
        """计算当前制造站内的标准化类技能数量"""
        if not self.配置数据 or not self.当前设施名:
            return 0
        设施数据 = self.配置数据.get(self.当前设施名, {})
        if not isinstance(设施数据, dict) or 设施数据.get("类型") != "制造站":
            return 0
        进驻干员列表 = 设施数据.get("进驻干员", [])
        count = 0
        for op in 进驻干员列表:
            name = op.get("名称", "")
            elite = op.get("精英等级", 0)
            if name in self.标准化技能拥有者:
                所需精英, 技能名 = self.标准化技能拥有者[name]
                if elite >= 所需精英:
                    count += 1
            # 星源E2有标准化·β
            if name == "星源" and elite >= 2:
                count += 1
        return count

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        """
        E0: 每个标准化类技能+5%生产力
        E2: 固定+25%生产力
        """
        if self.精英等级 >= 2:
            return 25.0
        else:
            标准数量 = self.计算标准化数量()
            return 标准数量 * 5.0'''

content = content.replace(old_水月, new_水月)

# 3. 修复泡泡 - E1仓库容量转生产力
old_泡泡 = '''class 泡泡(干员基类):
    """
    明日方舟 泡泡 基建技能类
        精英0: 囤积者 - 进驻制造站时，仓库容量上限+10，心情每小时消耗-0.25...
    精英1: 大就是好！ - 进驻制造站时，当前制造站内干员根据自身提升的仓库容量提供一定的生产力。提升16格以下的，每格仓库容量...
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器):
        super().__init__(精英等级, 特殊变量)
        self.特殊变量列表 = []
        if 精英等级 >= 2:
            self.基本属性 = {"生产力": 0}
        else:
            self.基本属性 = {}

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        return self.基本属性.get("生产力", 0)'''

new_泡泡 = '''class 泡泡(干员基类):
    """
    明日方舟 泡泡 基建技能类
        精英0: 囤积者 - 进驻制造站时，仓库容量上限+10，心情每小时消耗-0.25
    精英1: 大就是好！ - 当前制造站内干员根据自身提升的仓库容量提供生产力
            16格以下: 每格+1%生产力
            16格及以上: 每格+2%生产力
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器, 配置数据: dict = None, 当前设施名: str = None):
        super().__init__(精英等级, 特殊变量, 配置数据, 当前设施名)
        self.特殊变量列表 = []
        # E0: 仓库容量+10，心情消耗-0.25
        self.基本属性 = {"仓库容量": 10, "心情消耗": -0.25}

    def 计算仓库容量加成(self) -> int:
        """计算当前制造站内所有干员提供的仓库容量总和"""
        if not self.配置数据 or not self.当前设施名:
            return 0
        设施数据 = self.配置数据.get(self.当前设施名, {})
        if not isinstance(设施数据, dict) or 设施数据.get("类型") != "制造站":
            return 0
        进驻干员列表 = 设施数据.get("进驻干员", [])
        总容量 = 0
        for op in 进驻干员列表:
            name = op.get("名称", "")
            elite = op.get("精英等级", 0)
            try:
                # 创建干员实例获取其仓库容量加成
                from .operator_classes import 创建干员实例
                干员实例 = 创建干员实例(name, elite, self.特殊变量存储器)
                if hasattr(干员实例, '基本属性'):
                    容量 = 干员实例.基本属性.get("仓库容量", 0)
                    总容量 += 容量
            except:
                pass
        return 总容量

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        """
        E1: 根据仓库容量计算生产力
        """
        if self.精英等级 < 1:
            return 0.0

        总容量 = self.计算仓库容量加成()
        if 总容量 < 16:
            return 总容量 * 1.0  # 每格+1%
        else:
            return 总容量 * 2.0  # 每格+2%'''

content = content.replace(old_泡泡, new_泡泡)

# 4. 修复泰拉大陆调查团 - 木天蓼依赖
old_泰拉大陆调查团 = '''class 泰拉大陆调查团(干员基类):
    """
    明日方舟 泰拉大陆调查团 基建技能类
        精英0 30级: 可靠的随从们 - 进驻制造站时，仓库容量上限+8，生产力+5%，同时每有1个木天蓼术语:木天蓼可影响可爱的艾露猫、可靠...
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器):
        super().__init__(精英等级, 特殊变量)
        self.特殊变量列表 = []
        self.基本属性 = {"生产力": 5}

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        return self.基本属性.get("生产力", 0)'''

new_泰拉大陆调查团 = '''class 泰拉大陆调查团(干员基类):
    """
    明日方舟 泰拉大陆调查团 基建技能类
        精英0 30级: 可靠的随从们 - 仓库容量上限+8，生产力+5%，每有1个木天蓼额外+5%生产力
            木天蓼由以下干员提供: 泰拉大陆调查团
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器):
        super().__init__(精英等级, 特殊变量)
        self.特殊变量列表 = ["木天蓼"]
        # 基础: 仓库容量+8，生产力+5%
        self.基本属性 = {"仓库容量": 8, "生产力": 5}

    def 计算特殊技能(self):
        """添加木天蓼变量供其他干员使用"""
        # 泰拉大陆调查团自身提供1个木天蓼
        self.特殊变量存储器.添加变量("木天蓼", 1, "泰拉大陆调查团(可靠的随从们)")

    def 计算效率(self) -> float:
        """
        基础+5%，每有1个木天蓼额外+5%
        """
        基础效率 = 5.0
        木天蓼数量 = self.特殊变量存储器.获取变量("木天蓼")
        return 基础效率 + 木天蓼数量 * 5.0'''

content = content.replace(old_泰拉大陆调查团, new_泰拉大陆调查团)

# 5. 修复海沫 - E0莱茵科技类技能联动
old_海沫 = '''class 海沫(干员基类):
    """
    明日方舟 海沫 基建技能类
        精英0: 意识兼容 - 进驻制造站时，当前制造站内所有莱茵科技类术语:莱茵科技类包含以下技能莱茵科技·α、莱茵科技·β、莱茵...
    精英2: 标准化·β - 进驻制造站时，生产力+25%...
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器):
        super().__init__(精英等级, 特殊变量)
        self.特殊变量列表 = []
        if 精英等级 >= 2:
            self.基本属性 = {"生产力": 25}
        else:
            self.基本属性 = {}

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        return self.基本属性.get("生产力", 0)'''

new_海沫 = '''class 海沫(干员基类):
    """
    明日方舟 海沫 基建技能类
        精英0: 意识兼容 - 当前制造站内所有莱茵科技类技能额外+5%生产力
            莱茵科技类: 莱茵科技·α、莱茵科技·β、莱茵科技·γ
    精英2: 标准化·β - 进驻制造站时，生产力+25%
    """
    # 莱茵科技类技能拥有者
    莱茵科技技能拥有者 = {
        "赫默": (0, "莱茵科技·α"),
        "白面鸮": (0, "莱茵科技·α"),
        "梅尔": (0, "莱茵科技·α"),
        "塞雷娅": (2, "莱茵科技·β"),
        "多萝西": (2, "莱茵科技·β"),
        "星源": (0, "莱茵科技·α"),
        "淬羽赫默": (2, "莱茵科技·γ"),
    }

    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器, 配置数据: dict = None, 当前设施名: str = None):
        super().__init__(精英等级, 特殊变量, 配置数据, 当前设施名)
        self.特殊变量列表 = []
        self.基本属性 = {}

    def 计算莱茵科技数量(self) -> int:
        """计算当前制造站内的莱茵科技类技能数量"""
        if not self.配置数据 or not self.当前设施名:
            return 0
        设施数据 = self.配置数据.get(self.当前设施名, {})
        if not isinstance(设施数据, dict) or 设施数据.get("类型") != "制造站":
            return 0
        进驻干员列表 = 设施数据.get("进驻干员", [])
        count = 0
        for op in 进驻干员列表:
            name = op.get("名称", "")
            elite = op.get("精英等级", 0)
            if name in self.莱茵科技技能拥有者:
                所需精英, 技能名 = self.莱茵科技技能拥有者[name]
                if elite >= 所需精英:
                    count += 1
            # 星源E2有莱茵科技·β
            if name == "星源" and elite >= 2:
                count += 1
        return count

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        """
        E0: 每个莱茵科技类技能额外+5%（为其他干员加成）
        E2: 固定+25%生产力
        """
        if self.精英等级 >= 2:
            return 25.0
        else:
            # E0时海沫自身不提供效率，但为其他干员提供加成
            # 这里返回0，实际加成通过特殊变量或其他机制实现
            return 0.0'''

content = content.replace(old_海沫, new_海沫)

# 6. 修复溯光星源 - E0莱茵科技类技能联动
old_溯光星源 = '''class 溯光星源(干员基类):
    """
    明日方舟 溯光星源 基建技能类
        精英0: 勘探背包 - 进驻制造站时，当前制造站内每个莱茵科技类术语:莱茵科技类包含以下技能莱茵科技·α、莱茵科技·β、莱茵...
    精英2: 莱茵科技·β - 进驻制造站时，生产力+25%...
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器):
        super().__init__(精英等级, 特殊变量)
        self.特殊变量列表 = []
        if 精英等级 >= 2:
            self.基本属性 = {"生产力": 25}
        else:
            self.基本属性 = {}

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        return self.基本属性.get("生产力", 0)'''

new_溯光星源 = '''class 溯光星源(干员基类):
    """
    明日方舟 溯光星源 基建技能类
        精英0: 勘探背包 - 当前制造站内每个莱茵科技类技能为自身+5%生产力
            莱茵科技类: 莱茵科技·α、莱茵科技·β、莱茵科技·γ
    精英2: 莱茵科技·β - 进驻制造站时，生产力+25%
    """
    # 莱茵科技类技能拥有者
    莱茵科技技能拥有者 = {
        "赫默": (0, "莱茵科技·α"),
        "白面鸮": (0, "莱茵科技·α"),
        "梅尔": (0, "莱茵科技·α"),
        "塞雷娅": (2, "莱茵科技·β"),
        "多萝西": (2, "莱茵科技·β"),
        "星源": (0, "莱茵科技·α"),
        "淬羽赫默": (2, "莱茵科技·γ"),
    }

    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器, 配置数据: dict = None, 当前设施名: str = None):
        super().__init__(精英等级, 特殊变量, 配置数据, 当前设施名)
        self.特殊变量列表 = []
        self.基本属性 = {}

    def 计算莱茵科技数量(self) -> int:
        """计算当前制造站内的莱茵科技类技能数量"""
        if not self.配置数据 or not self.当前设施名:
            return 0
        设施数据 = self.配置数据.get(self.当前设施名, {})
        if not isinstance(设施数据, dict) or 设施数据.get("类型") != "制造站":
            return 0
        进驻干员列表 = 设施数据.get("进驻干员", [])
        count = 0
        for op in 进驻干员列表:
            name = op.get("名称", "")
            elite = op.get("精英等级", 0)
            if name in self.莱茵科技技能拥有者:
                所需精英, 技能名 = self.莱茵科技技能拥有者[name]
                if elite >= 所需精英:
                    count += 1
            # 星源E2有莱茵科技·β
            if name == "星源" and elite >= 2:
                count += 1
        return count

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        """
        E0: 每个莱茵科技类技能+5%生产力
        E2: 固定+25%生产力
        """
        if self.精英等级 >= 2:
            return 25.0
        else:
            莱茵数量 = self.计算莱茵科技数量()
            return 莱茵数量 * 5.0'''

content = content.replace(old_溯光星源, new_溯光星源)

# 7. 修复烈夏 - E2古米贸易站判断
old_烈夏 = '''class 烈夏(干员基类):
    """
    明日方舟 烈夏 基建技能类
        精英2: 患难拍档 - 进驻制造站时，若古米在贸易站，则作战记录类配方的生产力+35%...
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器):
        super().__init__(精英等级, 特殊变量)
        self.特殊变量列表 = []
        self.基本属性 = {"生产力": 35}

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        return self.基本属性.get("生产力", 0)'''

new_烈夏 = '''class 烈夏(干员基类):
    """
    明日方舟 烈夏 基建技能类
        精英2: 患难拍档 - 若古米在贸易站，则作战记录类配方+35%生产力
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器, 配置数据: dict = None):
        super().__init__(精英等级, 特殊变量, 配置数据)
        self.特殊变量列表 = []
        self.基本属性 = {}

    def 检测古米在贸易站(self) -> bool:
        """检测古米是否在贸易站工作"""
        if not self.配置数据:
            return False
        for 设施名, 设施数据 in self.配置数据.items():
            if isinstance(设施数据, dict) and 设施数据.get("类型") == "贸易站":
                进驻干员列表 = 设施数据.get("进驻干员", [])
                if any(op.get("名称") == "古米" for op in 进驻干员列表):
                    return True
        return False

    def 计算特殊技能(self):
        pass

    def 计算效率(self, 配方类型: str = "") -> float:
        """
        E2: 古米在贸易站时，作战记录配方+35%
        """
        if self.精英等级 >= 2 and 配方类型 == "作战记录":
            if self.检测古米在贸易站():
                return 35.0
        return 0.0'''

content = content.replace(old_烈夏, new_烈夏)

# 8. 修复玛露西尔 - E2魔物料理依赖
old_玛露西尔 = '''class 玛露西尔(干员基类):
    """
    明日方舟 玛露西尔 基建技能类
        精英0: 差遣使魔·α - 进驻制造站时，生产力+20%...
    精英2: 差遣使魔·β - 进驻制造站时，生产力+30%...
    精英2: 意想不到的美味 - 进驻制造站时，每1点魔物料理术语:魔物料理由以下干员的基建技能提供森西+1%生产力...
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器):
        super().__init__(精英等级, 特殊变量)
        self.特殊变量列表 = []
        if 精英等级 >= 2:
            self.基本属性 = {"生产力": 30}
        else:
            self.基本属性 = {}

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        return self.基本属性.get("生产力", 0)'''

new_玛露西尔 = '''class 玛露西尔(干员基类):
    """
    明日方舟 玛露西尔 基建技能类
        精英0: 差遣使魔·α - 进驻制造站时，生产力+20%
    精英2: 差遣使魔·β - 进驻制造站时，生产力+30%
    精英2: 意想不到的美味 - 每1点魔物料理+1%生产力
            魔物料理由森西(E2)提供
    """
    def __init__(self, 精英等级: int, 特殊变量: 特殊变量存储器):
        super().__init__(精英等级, 特殊变量)
        self.特殊变量列表 = []
        self.基本属性 = {}

    def 计算特殊技能(self):
        pass

    def 计算效率(self) -> float:
        """
        E0: +20%生产力
        E2: +30%生产力，每1点魔物料理额外+1%
        """
        if self.精英等级 >= 2:
            基础效率 = 30.0
            魔物料理 = self.特殊变量存储器.获取变量("魔物料理")
            return 基础效率 + 魔物料理 * 1.0
        else:
            return 20.0'''

content = content.replace(old_玛露西尔, new_玛露西尔)

# 写入文件
with open('/Users/altria/Documents/code/arkproject/building-calculator/core/operator_classes.py', 'w', encoding='utf-8') as f:
    f.write(content)

print("修复完成！")
print("\n修复内容：")
print("1. 杏仁 - E2黑钢国际联动（每名+2%）")
print("2. 水月 - E0标准化类技能联动（每个+5%）")
print("3. 泡泡 - E1仓库容量转生产力（16格以下每格+1%，以上每格+2%）")
print("4. 泰拉大陆调查团 - 木天蓼依赖（每个+5%）")
print("5. 海沫 - E0莱茵科技类技能联动（每个额外+5%）")
print("6. 溯光星源 - E0莱茵科技类技能联动（每个+5%）")
print("7. 烈夏 - E2古米贸易站判断（满足条件+35%）")
print("8. 玛露西尔 - E2魔物料理依赖（每点+1%）")
